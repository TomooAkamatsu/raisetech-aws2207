- name: Git clone rails-app
  git:
    repo: https://github.com/yuta-ushijima/raisetech-live8-sample-app.git
    dest: /var/www/raisetech-live8-sample-app

- name: Install bundle and rails
  become: yes
  shell: bash -lc "bundle install"
  args:
    chdir: /var/www/raisetech-live8-sample-app

- name: change authority
  shell: bash -lc "sudo chown -R ec2-user ."
  args:
    chdir: /var/www/raisetech-live8-sample-app/tmp

- name: start
  shell: bash -lc "{{ item }}"
  with_items:
    - rails db:create
    - rails db:migrate
    - rails s -p 3000 -b 0.0.0.0
  args:
    chdir: /var/www/raisetech-live8-sample-app

  # cmd: |
  #   bash -lc "bundle install"
  #   bash -lc "gem install rails -v "6.1.6.1”"
  # chdir: /var/www/raisetech-live8-sample-app
